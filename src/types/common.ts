// src/types/common.ts

/**
 * Placeholder for common types shared across different API resources.
 * Examples could include pagination structures, standard error formats,
 * or common identifier types if applicable.
 */

// Example: Maybe a standard pagination response structure?
// export interface PaginatedResponse<T> {
//   items: T[];
//   totalCount: number;
//   page: number;
//   pageSize: number;
// }

// Example: Common ID type
// export type UUID = string;

// We will populate this file further as we analyze the Go API handlers
// and identify recurring patterns in request/response structures. 